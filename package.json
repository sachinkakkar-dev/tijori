{
  "name": "tijori-server",
  "version": "1.0.0",
  "description": "Tijori family vault relay — Google OAuth, families, invites, roles, encrypted-at-rest store-and-forward.",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch --env-file=.env server.js",
    "start": "node --env-file=.env server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "google-auth-library": "^9.11.0",
    "ws": "^8.17.1"
  }
}